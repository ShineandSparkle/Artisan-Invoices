import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.artisanpparels.artisan',
  appName: 'artisan-invoicess',
  webDir: 'dist'
};

export default config;
